
import { quotesWithId } from './CreateQuoteCard'

function CustomQuoteCard(quotesWithId) {

  // const pickedQuote = (quotesWithId, chosenQuote) => {
  //   quotesWithId.filter(quote => {
  //     if (quote.id === chosenQuote) {
  //       console.log(pickedQuote)
  //     }
  //   })
  // }

  // pickedQuote()

  console.log(quotesWithId)

  return (
    <h1>udhdshs</h1>
    // <section className="hero quote-card background">
    //   {props.quotesWithId ? (
    //     <div className="hero-body text-css">
    //       <p className="title">{pickedQuote.text}</p>
    //       <p className="subtitle">{pickedQuote.author}</p>
    //     </div>
          
    //   ) : (
    //     <p>loading...</p>
    //   )
    //   }
    // </section>
  )
}

export default CustomQuoteCard